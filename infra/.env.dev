# Desenvolvimento local — valores inseguros DE PROPÓSITO, só para dev.
# Este arquivo É versionado. Referenciado por infra/docker-compose.dev.yml (env_file).
# Uso: docker compose --env-file infra/.env.dev -f infra/docker-compose.dev.yml up --build

POSTGRES_USER=login
POSTGRES_PASSWORD=senha
POSTGRES_DB=pcs3443

JWT_SECRET_KEY=segredo-dev-troque-em-producao

BACKEND_CORS_ORIGINS=["http://localhost:3000"]